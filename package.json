{
  "name": "extension-collage-maker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node ../utils/generate-extension-icons.js && vite build && ../utils/build-tasks.sh '#FF0000'",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.23",
    "vite": "^5.1.4"
  }
}
